// Models/LimitsDisplayMode.cs
namespace RC_GUI_WATS.Models
{
    public enum LimitsDisplayMode
    {
        Chronological,  // Kolejność chronologiczna (kolejność otrzymywania)
        Hierarchical    // Hierarchiczna (jak w zapisie do pliku: ALL -> Types -> Groups -> ISIN)
    }
}